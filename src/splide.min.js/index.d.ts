import { Splide, Options } from "@splidejs/splide";

declare const SplideClass = Splide;

declare const createSplide = (
  target: string | HTMLElement,
  options?: Options
) => Splide;

export { Splide, SplideClass };
